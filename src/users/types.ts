export interface RegisteredUsers {
    _id?: string;
    name: string;
    lastName: string;
    userName: string;
    email: string;
    password: string;
    refreshToken: string;
    bio: string;
    image: string;
}